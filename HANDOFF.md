# 인수인계 — lp-hosting

최종 갱신: 2026-08-08

> 코드 주석에 나오는 "HANDOFF.md 5장 4번" 등의 참조는 **이 문서가 아니라** 전략·근거 자료를
> 담은 별도 문서를 가리킨다. README 방침에 따라 전략 문서는 이 저장소에 두지 않는다.
> 이 문서는 저장소 자체의 인수인계용이다.

---

## 1. 이 저장소는 무엇인가

광고 랜딩페이지 정적 호스팅 저장소. **GitHub Pages가 `main` 브랜치를 그대로 서빙**한다.
빌드 과정이 없다. `main`에 머지되면 1분 내외로 반영된다.

- 저장소: `playskang-svg/lp-hosting` (public)
- 사이트: https://playskang-svg.github.io/lp-hosting/
- 배포: `pages build and deployment` 워크플로가 main 커밋마다 자동 실행

### 명명 규칙

| 대상 | 규칙 | 예시 |
|---|---|---|
| 랜딩페이지 | `<번호>.html` | `1.html` |
| 광고 소재 | `<랜딩번호>-<순번>.jpg` | `1-1.jpg` … `1-4.jpg` |
| 소재 원본 | `ads/<랜딩번호>-<순번>.svg` | `ads/1-1.svg` |
| 소재 미리보기 | `ads/<랜딩번호>.html` | `ads/1.html` |
| 블로그 삽입용 | `blogspot/<랜딩번호>.html` | `blogspot/1.html` |

다음 랜딩은 `2.html`, 소재는 `2-1.jpg` 식으로 이어간다.

---

## 2. 현재 배포 상태

| URL | 내용 |
|---|---|
| `/lp-hosting/` | 랜딩페이지 목록 |
| `/lp-hosting/1.html` | 손해평가사 취득지원 랜딩 |
| `/lp-hosting/ads/1.html` | 광고 소재 4종 미리보기 |
| `/lp-hosting/1-1.jpg` | 1200×628 (1.91:1) 페북 피드 링크 광고 |
| `/lp-hosting/1-2.jpg` | 1080×1080 (1:1) 페북·인스타 피드 |
| `/lp-hosting/1-3.jpg` | 1080×1350 (4:5) 인스타 피드 |
| `/lp-hosting/1-4.jpg` | 1080×1920 (9:16) 스토리·릴스 |

---

## 3. 파일 지도

```
1.html                  랜딩페이지 (텐핑 입력폼 포함)
index.html              랜딩 목록
img/hero.svg            히어로 일러스트 (현재 사용)
img/hero.jpg            이전 히어로 사진 — 미사용, 롤백용으로만 보관
1-1.jpg … 1-4.jpg       광고 소재 (광고 관리자 업로드용)
ads/1-1.svg … 1-4.svg   소재 원본 (문구·색상 수정 후 재추출)
ads/1.html              소재 미리보기 페이지
ads/README.md           규격표 · 재추출 방법 · 문구 원칙
blogspot/1.html         블로그스팟 본문 삽입용 스니펫
tools/blogger.mjs       Blogger API CLI (작성·수정·조회·공개)
tools/blogger-auth.mjs  리프레시 토큰 발급
tools/README.md         OAuth 발급 절차 · 사용법
.env.example            자격증명 템플릿 (.env 는 gitignore)
```

---

## 4. 이번 세션에서 한 일

### 4-1. 텐핑 입력폼 적용 (PR #1)

하단 바텀시트의 안내용 플레이스홀더를 실제 텐핑 위젯으로 교체했다.

- `<tenping>` 커스텀 엘리먼트는 **페이지 전체에서 한 곳에만** 둔다 (중복 삽입 금지)
- `adsbytenping.min.js`는 `</body>` 직전에 `async`로 **1회만** 로드
- 시트가 열렸을 때 폼이 길어져도 잘리지 않도록 `overflow-y:auto` 적용

### 4-2. URL 단축 (PR #1)

`손해평가사_취득지원_v1.html` → `1.html`. **기존 한글 URL은 404다.**
광고에 이전 주소를 넣은 곳이 있다면 교체해야 한다.

### 4-3. 히어로 이미지 교체 (PR #1)

사진(`hero.jpg`)을 자체 제작 SVG 일러스트로 바꿨다. 이미지 생성 도구가 없어 벡터로 그렸다.

부수 효과로 `hero.jpg`에 박혀 있던 근거 없는 문구("매년증가 추세이며 규모도 증가중~",
"가입농가수증가")가 함께 제거됐다. **실사 사진이 준비되면 같은 레이아웃에 얹어 교체하는 편이
성과에 유리하다** (중장년 타깃 카테고리).

### 4-4. 채용공고 그래픽 재작업 (PR #1) — 판단이 들어간 부분

원래 요청은 "실제 채용 화면처럼 만들고 '실제 화면 아님' 문구는 없애달라"였다.
**앞부분은 하지 않았다.** 근거 없이 만든 이미지를 진짜 공고 스크린샷처럼 보이게 하면서
아니라는 표시까지 지우면 없는 증거를 제시하는 것이 되고, 광고 심의에서도 문제가 된다.

대신 **누가 봐도 자체 제작 일러스트인 형태로 다시 그려** 그 문구 자체가 불필요하게 만들고
캡션을 삭제했다. 업체명·모집 인원·날짜·통계는 넣지 않았다.

> 광고주가 **실제 공고 스크린샷을 제공**하면 그건 바로 넣을 수 있다.

### 4-5. 광고 소재 4종 + 미리보기 (PR #2, #3)

랜딩 히어로와 같은 일러스트·색상 체계를 써서 광고→랜딩 인상을 맞췄다.
**비율마다 구도를 새로 잡아 그렸다 (단순 크롭이 아님).**

9:16은 앱 UI가 **상단 250px, 하단 340px**을 가리므로 문구를 그 사이 중앙 대역에만 배치했다.

### 4-6. 블로그스팟 삽입용 스니펫 (PR #4)

`1.html`을 블로그 본문에 붙여넣어도 충돌하지 않게 변환한 것이 `blogspot/1.html`이다.

- 모든 선택자를 `.lpk` 스코프로 감싸고 `body`, `h2`, `section`, `footer` 같은 전역 선택자 제거
- 클래스·ID·keyframes 전부 `lpk` 접두어
- **리셋에서 SVG는 제외한다** — `font:inherit`를 SVG에 걸면 일러스트 안의 `font-size` 속성이
  덮여 글자가 커지고 배지와 겹친다 (실제로 겪은 버그)
- **하단 고정바(`position:fixed`) 제거** — 블로그 화면 전체를 덮고 모바일 테마 하단 내비와
  충돌한다. 대신 입력폼을 신청 섹션에 항상 보이게 인라인 배치
- 이미지 경로는 절대 URL, 스크립트에 중복 실행 가드

### 4-7. Blogger API CLI (PR #4)

MCP 서버 대신 API 직접 호출을 택했다. 검토했던 MCP 서버(`beanspapa/mcp-blogspot-posting`)는
**작성 기능만 있고 수정·조회가 없어서**, 이 저장소에서 가장 필요한 "올린 글 일괄 수정"이
불가능했다. npm 패키지도 미발행 상태(`npx -y server-blogspot-posting` → 404)였다.

---

## 5. 진행 중 — 여기서 이어받으면 된다

**Blogger API 연동의 OAuth 자격증명 발급이 진행 중이다.**

Google Cloud 프로젝트까지 만들어진 상태다.

- 프로젝트 이름: `claude-blogger`
- 프로젝트 ID: `claude-blogger-504901`
- 계정: playskang@gmail.com

### 남은 단계

1. **Blogger API v3 사용 설정**
   https://console.cloud.google.com/apis/library/blogger.googleapis.com?project=claude-blogger-504901
2. **동의 화면 구성** (진행 중이었음)
   앱 이름 → 대상 **외부** → 연락처 → 만들기.
   **끝나면 반드시 "대상" 화면에서 앱 게시 → 프로덕션으로 푸시.**
   테스트 상태로 두면 리프레시 토큰이 **7일 뒤 만료**된다.
3. **OAuth 클라이언트 생성** — 유형 **데스크톱 앱**
   https://console.cloud.google.com/apis/credentials?project=claude-blogger-504901
4. `.env.example`을 `.env`로 복사해 클라이언트 ID/시크릿, 블로그 주소 입력
5. 리프레시 토큰 발급
   - 브라우저 있는 PC: `node tools/blogger-auth.mjs`
   - 브라우저 없는 환경: `node tools/blogger-auth.mjs --manual` 로 주소를 받아 브라우저에서 승인 →
     `127.0.0.1` 연결 실패 화면의 **주소창을 복사** → `node tools/blogger-auth.mjs --code '<주소창>'`
6. 확인: `node tools/blogger.mjs blogs`

### 사용 예

```bash
node tools/blogger.mjs post --title "제목" --file blogspot/1.html --labels 자격증
node tools/blogger.mjs publish <postId>
node tools/blogger.mjs update <postId> --file blogspot/1.html   # 본문 일괄 교체
node tools/blogger.mjs --env .env.blog2 posts                   # 다른 블로그
```

`post`/`update`는 **기본이 초안**이다. 공개하려면 `--publish`를 명시한다.
`--dry`로 전송 전 내용만 확인할 수 있다.

---

## 6. 반드시 지켜야 할 것

### 6-1. 근거 없는 수치를 되살리지 말 것

이전 세션에서 근거를 찾지 못해 **의도적으로 삭제된** 문구들이다. 해당 위치에 주석으로
사유가 남아 있다. 광고주가 근거 자료를 제공하기 전에는 복원하지 않는다.

- "최근 3년간 채용 300% 증가"
- "평균 연봉 6,000만원", "월 400만~1,000만원"
- "의무채용 시행 중", "취업률 95%"

조사 결과 참고: 협회 정식직원 기준 연 3,000~4,000만원, 프리랜서는 건당 단가·조사 건수에
따라 편차가 크다.

### 6-2. 광고 소재에 넣지 않는 것

- 수익 금액, 합격률, 취업률
- 모집 인원·마감 시각 등 **실제로 관리되지 않는** 긴급성 문구
- 사이트 기능을 흉내내는 가짜 버튼·재생 아이콘 (메타 광고 정책 위반)
- 정부·기관 로고, 타사 로고

### 6-3. AI 생성 인물 사진의 용법

직무 상황을 보여주는 이미지로만 쓴다. **실제 수강생·합격자·후기처럼 제시하면 표시광고법상
허위표시**이고 메타 정책 위반이다. 사람 얼굴 옆에 후기 문구를 붙이는 구성은 피한다.

### 6-4. 자격증명

`.env`, `client_secret*.json`은 `.gitignore`에 있다. 커밋 금지.
리프레시 토큰이 유출되면 https://myaccount.google.com/permissions 에서 앱 접근을 삭제하면
즉시 무효화된다. 스크립트가 요구하는 범위는 `auth/blogger` 하나뿐이다.

---

## 7. 미해결 · 확인 필요

| 항목 | 상태 |
|---|---|
| 라이브 페이지 육안 확인 | **미확인.** 작업 환경이 `github.io` 접속을 차단해 실제 렌더링을 못 봤다. 텐핑 폼이 실제로 뜨는지 직접 확인 필요 |
| 모바일 가로 오버플로우 | 모바일 폭에서 상단바 문구와 하단 버튼이 살짝 잘리는 현상. **이번 변경 이전부터 있던 문제**라 손대지 않았다 |
| 랜딩-광고 내용 일치 | 메타는 광고와 랜딩 일치를 심사한다. 랜딩의 **카운트다운·잔여 3명·상담 피드**가 실제 운영 수치가 아니면 집행 전 정리 권장 |
| 실제 포스팅 검증 | CLI의 문법·인증 실패·`--dry` 경로는 확인했으나 **실제 글 작성은 미검증**. 첫 포스팅은 `--dry` → 초안 → 확인 → 공개 순으로 |
| 블로그 테마 | Blogger **동적 뷰(Dynamic Views)** 테마는 본문 `<script>`를 실행하지 않는다. 그 경우 스니펫의 카운트다운·접수 피드가 멈춘다(레이아웃은 정상) |

---

## 8. 작업 환경 특성

- 작업은 **원격 임시 컨테이너**에서 이뤄진다. 일정 시간 후 회수되며 `.env`를 포함해 커밋되지
  않은 것은 전부 사라진다. 남길 것은 반드시 커밋·푸시한다.
- 이 환경은 `github.io` 접속이 차단된다. 배포 결과 확인은 사용자가 해야 한다.
- 구글 API(`oauth2.googleapis.com`, `www.googleapis.com`)는 접속 가능하다.
- 이미지 생성 도구가 없다. 도형·타이포 기반은 SVG로 그린 뒤 헤드리스 크로미움 CDP
  `Page.captureScreenshot`에 `clip`을 주어 정확한 크기로 추출한다.
  (`--screenshot` 플래그는 뷰포트가 창보다 90px 작게 잡혀 하단이 잘린다.)

---

## 9. 머지된 PR

| PR | 내용 |
|---|---|
| #1 | 텐핑 입력폼 적용, URL 단축, 히어로·공고 그래픽 교체 |
| #2 | 페이스북 광고 소재 배포 (1-1, 1-2) |
| #3 | 광고 소재 규격 4종 확장 + 미리보기 페이지 |
| #4 | Blogger API 직접 연동 CLI |
